services:
  streamlit:
    build:
      dockerfile: Dockerfile
      context: .
    container_name: streamlit-app
    cpus: 2
    mem_limit: 4096m
    ports:
      - "8501:8501"
    volumes:
      - ".:/app:rw"
